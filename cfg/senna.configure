#!/bin/bash
cd $HOME
#
# install libev into $HOME/opt
# set LD_LIBRARY_PATH to point 
#git clone https://github.com/dblarkins/portals4.git
#git checkout pdht-portals
cd portals4
#./autogen.sh
./configure --prefix=$HOME/opt --enable-transport-ib --enable-fast --enable-zero-mrs --enable-me-triggered --enable-transport-shmem --enable-unordered-matching
